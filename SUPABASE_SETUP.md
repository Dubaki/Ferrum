# Настройка Supabase Storage для загрузки чертежей

## Что такое Supabase?

Supabase - это бесплатная платформа для Backend-as-a-Service с хранилищем файлов.

**Бесплатный план включает:**
- 1 GB хранилища
- Неограниченный трафик
- Регистрация БЕЗ карты!
- Простая настройка за 5 минут

---

## Шаг 1: Регистрация в Supabase

1. Перейдите на https://supabase.com/
2. Нажмите **"Start your project"**
3. Войдите через GitHub или создайте аккаунт с email
4. ✅ **Карта НЕ требуется!**

---

## Шаг 2: Создать новый проект

1. В Dashboard нажмите **"New Project"**
2. Заполните:
   - **Name**: `planfer` (или любое имя)
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион (например, Frankfurt)
3. Нажмите **"Create new project"**
4. ⏳ Подождите 1-2 минуты пока проект создается

---

## Шаг 3: Создать Storage Bucket

1. В левом меню откройте **"Storage"**
2. Нажмите **"Create a new bucket"**
3. Заполните:
   - **Name**: `planfer-drawings` ⚠️ **ТОЧНО ТАК!** (должно совпадать с кодом)
   - **Public bucket**: ✅ **ВКЛ** (файлы должны быть доступны по ссылке)
   - **File size limit**: `10 MB` (опционально)
   - **Allowed MIME types**: `application/pdf` (опционально)
4. Нажмите **"Create bucket"**

---

## Шаг 4: Получить API ключи

1. В левом меню откройте **"Project Settings"** (шестеренка ⚙️)
2. Выберите **"API"**
3. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public** ключ (длинная строка начинающаяся с `eyJ...`)

📋 **Сохраните эти значения** - они понадобятся дальше!

---

## Шаг 5: Настроить RLS (Row Level Security) политики

⚠️ **ВАЖНЫЙ ШАГ!** Без этого загрузка не будет работать.

### 5.1. Включить публичный доступ

1. Откройте **Storage** → выберите bucket `planfer-drawings`
2. Перейдите на вкладку **"Policies"**
3. Нажмите **"New Policy"**

### 5.2. Создать политику для загрузки (INSERT)

1. Выберите **"Create a policy from scratch"**
2. Заполните:
   - **Policy name**: `Allow public uploads`
   - **Allowed operation**: выберите **INSERT**
   - **Target roles**: оставьте `public`
   - **USING expression**: вставьте: `true`
3. Нажмите **"Review"** → **"Save policy"**

### 5.3. Создать политику для чтения (SELECT)

1. Снова **"New Policy"** → **"Create a policy from scratch"**
2. Заполните:
   - **Policy name**: `Allow public downloads`
   - **Allowed operation**: выберите **SELECT**
   - **Target roles**: оставьте `public`
   - **USING expression**: вставьте: `true`
3. Нажмите **"Review"** → **"Save policy"**

### 5.4. Создать политику для удаления (DELETE)

1. Снова **"New Policy"** → **"Create a policy from scratch"**
2. Заполните:
   - **Policy name**: `Allow public deletes`
   - **Allowed operation**: выберите **DELETE**
   - **Target roles**: оставьте `public`
   - **USING expression**: вставьте: `true`
3. Нажмите **"Review"** → **"Save policy"**

✅ Должно быть **3 политики**: INSERT, SELECT, DELETE

---

## Шаг 6: Добавить переменные окружения

Откройте файл `.env` в корне проекта и добавьте:

```env
# Supabase для загрузки чертежей
VITE_SUPABASE_URL=ваш_project_url
VITE_SUPABASE_ANON_KEY=ваш_anon_public_key
```

### Пример:

```env
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

⚠️ **ВАЖНО**: После изменения `.env` нужно **перезапустить** сервер разработки:
```bash
# Остановите (Ctrl+C) и запустите заново:
npm run dev
```

---

## Шаг 7: Проверка работы

1. Перезапустите приложение (`npm run dev`)
2. Откройте любой **производственный заказ** (не товарный!)
3. Внизу карточки заказа появится секция **"Чертежи"**
4. Нажмите **"Загрузить"** и выберите PDF файл (до 10MB)
5. ✅ Файл должен загрузиться без ошибок
6. Нажмите на кнопку скачивания (стрелка вниз)
7. ✅ PDF должен открыться в новой вкладке

---

## Часто задаваемые вопросы

### ❓ Нужна ли банковская карта?

Нет! Бесплатный план не требует карты.

### ❓ Что если файлов будет больше 1GB?

Supabase автоматически предупредит вас. Можно:
1. Удалить старые файлы через Supabase Dashboard
2. Перейти на платный план (от $25/месяц за 100GB)

### ❓ Как удалить файлы вручную?

1. Откройте Supabase Dashboard → Storage → `planfer-drawings`
2. Найдите файл и нажмите на него
3. Нажмите **Delete**

### ❓ Где физически хранятся файлы?

На серверах Supabase (AWS). Файлы НЕ хранятся в вашей базе Firestore - там только метаданные (имя, URL, размер).

### ❓ Можно ли ограничить доступ?

Да, но требуется настроить более сложные RLS политики. Для начала работы публичный доступ безопасен (файлы доступны только тем, у кого есть прямая ссылка).

### ❓ Ошибка при загрузке файла

**Проверьте:**
1. Переменные в `.env` правильные
2. Перезапустили `npm run dev` после изменения `.env`
3. Bucket называется **точно** `planfer-drawings`
4. Bucket **публичный** (Public bucket = ON)
5. Созданы **все 3 RLS политики** (INSERT, SELECT, DELETE)

**Откройте консоль браузера (F12)** - там будет точное сообщение об ошибке.

---

## Структура хранения

Файлы организованы по папкам:

```
planfer-drawings/
└── drawings/
    ├── order_abc123/
    │   ├── 1234567890_xyz.pdf
    │   └── 1234567891_abc.pdf
    └── order_xyz789/
        └── 1234567892_def.pdf
```

Каждый заказ имеет свою папку, файлы имеют уникальные имена (timestamp + random).

---

## Поддержка

Если что-то не работает:

1. Проверьте `.env` файл
2. Перезапустите `npm run dev`
3. Откройте консоль браузера (F12) - там будут ошибки
4. Убедитесь что bucket публичный и есть все 3 RLS политики
5. Проверьте что bucket называется `planfer-drawings`

Всё ещё не работает? Проверьте код в `src/utils/supabaseStorage.js`.

---

**Готово!** Теперь можно загружать PDF чертежи для заказов. 🚀
